import BackHeader from './BackHeader';
import MainHeader from './MainHeader';
import GlobalHeader from './GlobalHeader';
import ModalHeader from './ModalHeader';

export { BackHeader, MainHeader, GlobalHeader, ModalHeader };
